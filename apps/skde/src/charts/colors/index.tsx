const maincolor: string = "171, 108, 166";
const nationalcolor: string = "120, 45, 135";
const pickedcolor: string = "3, 63, 133";

export const mapColors: string[] = [
  `rgba(${maincolor}, 0.25)`,
  `rgba(${maincolor}, 0.5)`,
  `rgba(${maincolor}, 0.75)`,
  `rgba(${maincolor}, 1.0)`,
];

export const abacusColors: string[] = [
  `rgba(${maincolor}, 0.8)`,
  `rgba(${nationalcolor}, 0.8)`,
  `rgba(${pickedcolor}, 0.8)`,
];

export const mainBarColors: string[] = [
  `rgba(${maincolor}, 1)`,
  `rgba(${maincolor}, 0.7)`,
  `rgba(${maincolor}, 0.4)`,
];

export const nationBarColors: string[] = [
  `rgba(${nationalcolor}, 1)`,
  `rgba(${nationalcolor}, 0.7)`,
  `rgba(${nationalcolor}, 0.4)`,
];

export const selectedBarColors: string[] = [
  `rgba(${pickedcolor}, 1)`,
  `rgba(${pickedcolor}, 0.7)`,
  `rgba(${pickedcolor}, 0.4)`,
];

export const linechartColors: string[] = [
  "rgb(0,193,159)",
  "rgb(135,24,157)",
  "rgb(255,103,31)",
];
