// Colour scheme
export const columnColour1 = "#F7FBFF";
export const columnColour2 = "#E0F1FF";
export const columnColour3 = "#F2F9FF";

// The checkbox component has a circular are that is highlighted on mouse hover.
// This is called a ripple and extends beyond the form control component.
// For alignment, the checkbox must be moved towards the right by the ripple offset.

// By inspection if the checkbox size is set to medium
const checkboxWidth = 18;
const rippleWidth = 42;
// Calculated offset
export const rippleOffset = (rippleWidth - checkboxWidth) / 2 - 1;

export const borderRadius = "8px";
export const marginTop = "8px";
