{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "startStrapiServer",
      "type": "shell",
      "command": "yarn",
      "args": ["develop", "--watch-admin"],
      "options": {
        "cwd": "${workspaceFolder}/apps/strapi/"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
